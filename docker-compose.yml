version: '3'
services:
  server:
    build: .
    links:
     - db

  client:
    build:
      dockerfile: Dockerfile.client
      context: .

  db:
    build:
      dockerfile: Dockerfile.db
      context: .
    volumes:
     - ./db:/app/db
